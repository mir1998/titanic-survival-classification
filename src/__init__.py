"""Titanic survival classification pipeline.

Modules
-------
data
    Kaggle download, loading of ``train.csv``, and the train/validation split.
preprocess
    Feature engineering and the scikit-learn preprocessing pipeline.
model
    The PyTorch classifier definition.
evaluate
    Metrics and plotting helpers shared by ``train.py`` and ``ds_app.py``.
"""
